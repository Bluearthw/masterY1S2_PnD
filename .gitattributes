# This file instructs git LFS to filter files matching *.oa, *.dm and *.png.
# The point of git LFS is to more appropriately deal with binary files
# since a diff-based system does not make sense for those.
*.oa filter=lfs diff=lfs merge=lfs -text
*.dm filter=lfs diff=lfs merge=lfs -text
*.png filter=lfs diff=lfs merge=lfs -text
